---
'@victor-software-house/exa-cli': minor
---

Add `exa auth login`, `status`, and `logout` with dependency-free OS credential storage. Interactive commands now resolve API keys from macOS Keychain, Linux Secret Service, or Windows DPAPI after command-line and environment credentials.
